Replacement of unicode characters:

Find and replace:

% % with \%
% ~ with $\sim$
% +\- with \pm
% * with \times
% - (dash) with - (minus) in math expressions
% >= with \geq
% # with \#
% α with $...\alpha...$

%UNITS
% μL with si{\uL}

% p-values: eg $p = 4 \times 10^{-4}$
%   \num{4e-4} is even better! %From siunitx package

% All figure references with eg \ref{fig:CC_fig1}